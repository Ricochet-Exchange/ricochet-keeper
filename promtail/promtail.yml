server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /tmp/positions.yaml

clients:
  - url: 34.249.231.194:3100/loki/api/v1/push


scrape_configs:
- job_name: dag_processor_manager
  static_config:
  - targets:
      - localhost
    labels:
      job: sag_processor_manager
      __path__: /var/log/dag_processor_manager.log
- job_name: scheduler_staging
  static_config:
  - target:
      - localhost
    labels:
      job: scheduler
      __path__: /var/log/scheduler/*/*.log
